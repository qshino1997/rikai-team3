<%= form_with(model: catogary, local: true) do |form| %>
  <% if catogary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(catogary.errors.count, "error") %> prohibited this catogary from being saved:</h2>

      <ul>
      <% catogary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :tenloai,"Thể loại sách mới" %>
    <%= form.text_field :tenloai %>
  </div>

  <div class="actions">
    <%= form.submit value:"Thêm" %>
  </div>
<% end %>
